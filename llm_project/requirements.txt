# Python runtime dependencies for AutoFPGA.
# Standard-library imports such as argparse/json/os/re/subprocess/unittest
# do not need to be installed by pip.
#
# External tools such as Vivado, ModelSim, Icarus Verilog, and Ollama are
# configured separately and are not pip-installable Python packages.

# HTTP calls to DeepSeek and local Ollama embedding endpoints.
requests>=2.31.0

# Persistent vector database for datasheet/RAG retrieval.
chromadb>=0.5.0

# PDF text/table extraction for datasheets.
pdfplumber>=0.11.0
